<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Invoice - {{invoiceNumber}}</title>
  <style>
    @font-face {
      font-family: 'Kalpurush';
      src: url('https://fonts.cdnfonts.com/s/15068/Kalpurush.woff') format('woff');
      font-weight: normal;
      font-style: normal;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Kalpurush', Arial, sans-serif;
      font-size: 12px;
      line-height: 1.6;
      color: #333;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }

    .header {
      text-align: center;
      margin-bottom: 30px;
      border-bottom: 3px solid #2c3e50;
      padding-bottom: 20px;
    }

    .header h1 {
      color: #2c3e50;
      font-size: 24px;
      margin-bottom: 5px;
    }

    .header h2 {
      color: #34495e;
      font-size: 18px;
      margin-bottom: 10px;
    }

    .header p {
      font-size: 11px;
      color: #666;
    }

    .invoice-info {
      display: flex;
      justify-content: space-between;
      margin-bottom: 30px;
    }

    .invoice-info div {
      flex: 1;
    }

    .info-box {
      background: #f8f9fa;
      padding: 15px;
      border-radius: 5px;
      margin-bottom: 20px;
    }

    .info-box h3 {
      color: #2c3e50;
      font-size: 14px;
      margin-bottom: 10px;
      border-bottom: 2px solid #3498db;
      padding-bottom: 5px;
    }

    .info-box p {
      margin: 5px 0;
      font-size: 12px;
    }

    .info-box strong {
      color: #2c3e50;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }

    table thead {
      background: #2c3e50;
      color: white;
    }

    table th {
      padding: 12px;
      text-align: left;
      font-weight: bold;
      font-size: 12px;
    }

    table td {
      padding: 10px 12px;
      border-bottom: 1px solid #ddd;
      font-size: 12px;
    }

    table tbody tr:nth-child(even) {
      background: #f8f9fa;
    }

    table tbody tr:hover {
      background: #e9ecef;
    }

    .text-right {
      text-align: right;
    }

    .text-center {
      text-align: center;
    }

    .summary-table {
      margin-left: auto;
      width: 400px;
      margin-top: 20px;
    }

    .summary-table td {
      padding: 8px 12px;
    }

    .summary-table .total-row {
      background: #2c3e50;
      color: white;
      font-weight: bold;
      font-size: 14px;
    }

    .footer {
      margin-top: 50px;
      padding-top: 20px;
      border-top: 2px solid #2c3e50;
      text-align: center;
      font-size: 11px;
      color: #666;
    }

    .highlight {
      background: #fff3cd;
      padding: 2px 5px;
      border-radius: 3px;
    }

    .status-unpaid {
      color: #dc3545;
      font-weight: bold;
    }

    .status-partial {
      color: #fd7e14;
      font-weight: bold;
    }

    .status-paid {
      color: #28a745;
      font-weight: bold;
    }

    .section-title {
      color: #2c3e50;
      font-size: 16px;
      margin: 30px 0 15px 0;
      padding-bottom: 5px;
      border-bottom: 2px solid #3498db;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>বাংলাদেশ হাই-টেক পার্ক কর্তৃপক্ষ</h1>
      <h2>Bangladesh Hi-Tech Park Authority (BHTPA)</h2>
      <p>ICT Tower, Agargaon, Dhaka-1207, Bangladesh</p>
      <p>Phone: +880-2-XXXXXXXX | Email: info@bhtpa.gov.bd</p>
    </div>

    <!-- Invoice Info -->
    <div class="invoice-info">
      <div>
        <p><strong>Invoice Number:</strong> {{invoiceNumber}}</p>
        <p><strong>Invoice Date:</strong> {{invoiceDate}}</p>
        <p><strong>Bill Month:</strong> {{formatBillMonth billMonth}}</p>
      </div>
      <div style="text-align: right;">
        <p><strong>Contract Number:</strong> {{contract.contractNumber}}</p>
      </div>
    </div>

    <!-- Client Information -->
    <div class="info-box">
      <h3>Bill To:</h3>
      <p><strong>Company:</strong> {{client.companyName}}</p>
      <p><strong>Contact Person:</strong> {{client.contactPerson}}</p>
      <p><strong>Address:</strong> {{client.address}}</p>
      <p><strong>Phone:</strong> {{client.phone}}</p>
      <p><strong>Email:</strong> {{client.email}}</p>
    </div>

    <!-- Contract Details -->
    <div class="info-box">
      <h3>Contract Details:</h3>
      <p><strong>Space Allocated:</strong> {{formatNumber contract.spaceInSqft}} sq.ft.</p>
      <p><strong>Rent Rate:</strong> ৳{{formatNumber contract.rentRate}}/sq.ft.</p>
      <p><strong>Service Charge Rate:</strong> ৳{{formatNumber contract.serviceChargeRate}}/sq.ft.</p>
    </div>

    <!-- Current Month Bill -->
    <h2 class="section-title">Current Month Charges ({{formatBillMonth currentBill.billMonth}})</h2>
    <table>
      <thead>
        <tr>
          <th>Description</th>
          <th class="text-right">Rate</th>
          <th class="text-right">Area (sq.ft.)</th>
          <th class="text-right">Amount (৳)</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>Rent</td>
          <td class="text-right">৳{{formatNumber contract.rentRate}}</td>
          <td class="text-right">{{formatNumber contract.spaceInSqft}}</td>
          <td class="text-right">৳{{formatNumber currentBill.rentAmount}}</td>
        </tr>
        <tr>
          <td>Service Charge</td>
          <td class="text-right">৳{{formatNumber contract.serviceChargeRate}}</td>
          <td class="text-right">{{formatNumber contract.spaceInSqft}}</td>
          <td class="text-right">৳{{formatNumber currentBill.serviceAmount}}</td>
        </tr>
        <tr style="font-weight: bold; background: #e9ecef;">
          <td colspan="3" class="text-right">Current Month Total:</td>
          <td class="text-right">৳{{formatNumber currentBill.monthlyTotal}}</td>
        </tr>
      </tbody>
    </table>

    <!-- Previous Arrears (if any) -->
    {{#if (gt arrears.length 0)}}
    <h2 class="section-title">Previous Outstanding Arrears (Rolling Basis - FIFO)</h2>
    <table>
      <thead>
        <tr>
          <th>Month</th>
          <th class="text-right">Bill Amount (৳)</th>
          <th class="text-right">Paid (৳)</th>
          <th class="text-right">Outstanding (৳)</th>
        </tr>
      </thead>
      <tbody>
        {{#each arrears}}
        <tr>
          <td>{{formatBillMonth billMonth}}</td>
          <td class="text-right">৳{{formatNumber monthlyTotal}}</td>
          <td class="text-right">৳{{formatNumber paidAmount}}</td>
          <td class="text-right status-unpaid">৳{{formatNumber remainingAmount}}</td>
        </tr>
        {{/each}}
        <tr style="font-weight: bold; background: #fff3cd;">
          <td colspan="3" class="text-right">Total Previous Arrears:</td>
          <td class="text-right">৳{{formatNumber totalArrears}}</td>
        </tr>
      </tbody>
    </table>
    {{/if}}

    <!-- Grand Total -->
    <table class="summary-table">
      <tr>
        <td>Current Month Charges:</td>
        <td class="text-right">৳{{formatNumber currentBill.monthlyTotal}}</td>
      </tr>
      {{#if (gt totalArrears 0)}}
      <tr>
        <td>Previous Arrears:</td>
        <td class="text-right">৳{{formatNumber totalArrears}}</td>
      </tr>
      {{/if}}
      <tr class="total-row">
        <td>GRAND TOTAL PAYABLE:</td>
        <td class="text-right">৳{{formatNumber grandTotal}}</td>
      </tr>
    </table>

    <!-- Payment Instructions -->
    <div class="info-box" style="margin-top: 30px;">
      <h3>Payment Instructions:</h3>
      <p><strong>Bank Name:</strong> [Bank Name]</p>
      <p><strong>Account Name:</strong> Bangladesh Hi-Tech Park Authority</p>
      <p><strong>Account Number:</strong> [Account Number]</p>
      <p><strong>Branch:</strong> [Branch Name]</p>
      <p style="margin-top: 10px;"><em>Please mention your Contract Number ({{contract.contractNumber}}) in the payment reference.</em></p>
      <p style="color: #dc3545; font-weight: bold; margin-top: 10px;">
        Payment follows FIFO principle: Oldest outstanding bills are cleared first.
      </p>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>This is a computer-generated invoice and does not require a signature.</p>
      <p>For any queries, please contact: billing@bhtpa.gov.bd</p>
      <p style="margin-top: 10px;">&copy; {{currentYear}} Bangladesh Hi-Tech Park Authority. All rights reserved.</p>
    </div>
  </div>

  <script>
    // Add current year
    document.addEventListener('DOMContentLoaded', function() {
      const yearElements = document.querySelectorAll('[data-year]');
      const currentYear = new Date().getFullYear();
      yearElements.forEach(el => el.textContent = currentYear);
    });
  </script>
</body>
</html>
